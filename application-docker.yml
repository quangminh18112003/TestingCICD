server:
  port: 8080

spring:
  datasource:
    # Use Docker service name 'mysql' instead of localhost
    url: jdbc:mysql://mysql:3306/uniclub
    username: root
    password: huytran123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  task:
    scheduling:
      enabled: true

# SendGrid configuration
sendgrid:
  api-key: ${SENDGRID_API_KEY:your-sendgrid-api-key-here}

app:
  mail:
    from: ${SENDER_EMAIL:no-reply@mail.tranhuy.dev}
    otp-expiration-minutes: 5
    max-attempts: 5

jwt:
  secret: uniclub-secret-key-for-jwt-token-generation-2024
  expiration: 86400000  # 24 hours in milliseconds

cloudinary:
  cloud_name: deooamndi
  api_key: 186558834811161
  api_secret: 7iKsQDdlEz-vPTEYyuePBiET7Sw

vnpay:
  tmn-code: ${VNPAY_TMN_CODE:CGEJ0TI4}
  hash-secret: ${VNPAY_HASH_SECRET:GNUPBN07YMSNSDZPJ1ZLBIKXVNDSOTUI}
  return-url: ${VNPAY_RETURN_URL:http://localhost/admin/payment/vnpay-return}
